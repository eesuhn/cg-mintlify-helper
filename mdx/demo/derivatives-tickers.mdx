---
openapi: api-reference/coingecko-demo.json get /derivatives
---

<Note>
  ### Note

  - Data for `open_interest` and `volume_24h` in the endpoint responses are in USD.
  - Cache / Update Frequency: every 30 seconds for all the API plans.
</Note>