## Usage

- Add `x-mint` config
    ```bash
    make add-mint
    ```

- Fetch markdown from ReadMe to generate Mintlify's MDX

    - Demo API:
        ```bash
        make mdx-demo
        ```

    - Pro API:
        ```bash
        make mdx-pro
        ```
